#JSGF V1.0;

/**
 * JSGF Grammar
 */

grammar grammar;

public <repo> = ( git | git status | git commit | git add | git push | git checkout | git clone);
public <buffer> = (copy | paste);
public <filesystem> = ( disk E | disk D | disk C );